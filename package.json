{
  "name": "permission-handler-patovica",
  "version": "1.5.0",
  "description": "Simple yet powerful role-based permission handler for TypeScript applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "prepare": "npm run build",
    "prepack": "npm run clean && npm run build",
    "check:pack": "npm pack --dry-run",
    "pack": "npm pack",
    "publish": "npm publish",
    "publish:dry": "npm publish --dry-run",
    "release:patch": "npm version patch && git push --follow-tags && npm run publish",
    "release:minor": "npm version minor && git push --follow-tags && npm run publish",
    "release:major": "npm version major && git push --follow-tags && npm run publish",
    "test": "echo \"No tests yet\""
  },
  "keywords": [
    "permissions",
    "authorization",
    "roles",
    "typescript",
    "rbac",
    "access-control"
  ],
  "author": "DarthKenar",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/DarthKenar/permission-handler-patovica"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.13.10",
    "rimraf": "^5.0.5"
  }
}
